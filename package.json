{
  "name": "vscode-run-on-me",
  "displayName": "Run On Me",
  "description": "This extension allows you to run custom binaries on current file.",
  "publisher": "WardenDigital",
  "repository": {
    "type": "git",
    "url": "https://github.com/WardenDigital/vscode-run-on-me-plugin"
  },
  "version": "0.0.5",
  "engines": {
    "vscode": "^1.106.2"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "files": [
    "dist/**",
    "src/views/**",
    "node_modules/@vscode/codicons/dist/**",
    "package.json",
    "README.md",
    "LICENSE.md"
  ],
  "contributes": {
    "commands": [
      {
        "command": "vscode-run-on-me.run-on-me",
        "title": "Run on me"
      },
      {
        "command": "vscode-run-on-me.settings",
        "title": "RunOnMe Settings"
      }
    ],
    "menus": {
      "editor/title/run": [
        {
          "command": "vscode-run-on-me.run-on-me",
          "group": "edit"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.106.2",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.2",
    "eslint": "^9.39.2",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.52.0"
  },
  "dependencies": {
    "@vscode/codicons": "^0.0.44"
  }
}
